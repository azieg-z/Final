
class mySquare {
  
    constructor(x, y, size, col)
    {
      this.x = x;
      this.y = y;
      this.size = size;
      this.col = col;
    }
    
    display()
    {
      square(this.x,this.y,this.size);
    }
    
  }
  
